[package]
name = "roast-core"
version = "0.1.0"
edition = "2021"
description = "Rust/WASM heat solver matching the Roast Solver NumPy oracle"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "=0.2.121"
