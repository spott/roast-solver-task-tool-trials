{
  "name": "roast-solver-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm": "bash web/scripts/build-wasm.sh",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm && npm run build:web",
    "build:web": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "npm run wasm && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
