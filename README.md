# Roast Solver experiment seed

This repository is the clean shared starting point for two independent Pi experiment variants. The implementation task and requirements are in [`PROJECT_PLAN.md`](PROJECT_PLAN.md).

The repository intentionally contains no implementation. Each experiment run works in its own detached worktree from this exact commit.
