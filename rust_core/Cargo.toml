[package]
name = "roast-core"
version = "0.1.0"
edition = "2021"
description = "Dependency-free Rust/WASM port of the Roast Solver reference kernel"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
panic = "abort"
